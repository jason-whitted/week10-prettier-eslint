function doSomethingLikeWorkAndStuff({street,city,state,zip}) {

  console.log(street, city, state, zip);

    // TODO: I would like to do some stuff with this function, but I'm not sure exactly what I want it to do yet, so I'll just put stuff in here and come back to it and then when I know what stuff I want it to do then I can make it do that stuff.  And stuff.

  let randoms = []
for(let i=0;i<5;i++)
{

  randoms.push(Math.floor(Math.random() * 1000))

}
  
  return arr.filter(n => n % 2 === 0).map(n => n.toString).reverse().join('|')
}